from setuptools import setup, find_packages

setup(
    name='loading_bar',
    version='0.1.0',
    packages=find_packages(),
    description='A simple loading bar for console applications',
    classifiers=[
        'Programming Language :: Python :: 3',
        'Operating System :: OS Independent',
    ],
    python_requires='>=3.6',
)
